{
  "name": "claude-ai-extension",
  "version": "0.1.0",
  "description": "Browser extension to capture claude.ai session token usage and feed it into the tc token counter",
  "private": true,
  "scripts": {
    "build": "web-ext build",
    "lint": "web-ext lint"
  }
}
